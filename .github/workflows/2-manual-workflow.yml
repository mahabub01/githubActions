name: Manual workflow

on:
  workflow_dispatch:


jobs:
    build:
        runs-on: ubuntu-latest
        steps:
            - name: first step
              run: echo "hello first step"

            - name: 2nd step
              run: echo "2nd step"

            - name: 3rd step
              run: echo "3rd step"